import { MyUIComponent } from '@repo/ui/gradient';

export default function Page() {
    return (
        <main className="flex min-h-screen flex-col items-center justify-center p-24">
            <MyUIComponent />
        </main>
    );
}
